import styled from "styled-components";

const Products = styled.div`
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 30px;
`;

const Styled = { Products };

export default Styled;
